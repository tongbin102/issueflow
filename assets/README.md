# assets/ — 静态资源目录

存放项目运行与文档所需的静态资源，按类型分子目录管理：

- `images/`：logo、文档插图、看板示例图等图片素材（SVG/PNG）。
- （未来可扩展）`fonts/`：自定义字体；`data/`：静态数据集/样例导入文件。

> 该目录不存放用户上传的缺陷附件（附件由后端存储在容器卷 `/data/attachments`，见 `src/backend` 配置）。
> 本目录内容会随仓库提交，请勿放入体积过大的二进制文件。
