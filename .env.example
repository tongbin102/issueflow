# 复制为 .env 后按需修改（docker compose 会自动读取）
# 不创建 .env 也能直接 docker compose up（下方均为默认值）

# MySQL
MYSQL_ROOT_PASSWORD=issueflow123
MYSQL_DB=issueflow
MYSQL_USER=root

# JWT（生产请务必改成足够随机的 32+ 字节密钥）
JWT_SECRET=issueflow-secret-key-2024-issueflow-jwt-secure-32bytes!!
JWT_EXPIRATION=7200
